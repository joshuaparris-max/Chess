# QA Report - 2026-06-15

## Release Gates

- TypeScript: passed
- Vitest: passed
- Playwright desktop/mobile: passed
- Production build: passed
- `git diff --check`: passed

## Covered Flows

- Mobile board remains square without horizontal overflow
- Play as Black flips orientation and lets Stockfish move first
- Timed games render both clocks
- Critical chess rules: castling, en passant, underpromotion, repetition
- Local archive cap and progress normalization
- Opening recognition and AI provider resilience
- Family theme persistence and family-room entry

## Post-Report Additions (verified separately)

The following landed after the gate run above and were verified with `tsc --noEmit`, a production
`next build`, and `npm run validate:puzzles` (all passed). Vitest and Playwright were not re-run for
these changes.

- 114-puzzle adult tactics trainer (generated and verified): difficulty bands, themes/phase tags, multi-move sequences with
  opponent auto-reply, black-to-move flip, three-level hints, difficulty filter, local progress
- Family puzzle bank expanded to 24 positions
- Selectable adult board piece sets (classic SVG, inverted, modern, outline, letters)
- Puzzle validator script and `docs/PUZZLE_SOURCES.md`

## Known Limitations

- AI provider behavior cannot be fully production-tested without configured Groq credentials.
- Cloud accounts and cross-device synchronization are not configured.
- Deep engine move classification and server-side analysis remain future work.
